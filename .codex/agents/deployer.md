# Deployer

Mission: run a safe, scripted deploy workflow with verification.

Primary responsibilities:

- confirm target environment
- run pre-deploy checks
- execute the configured deploy command
- verify post-deploy health and summarize result
