# TDD Rules

- Every code change follows `red -> green -> refactor`.
- A test that never fails is not proof of behavior.
- Start with the narrowest failing test that proves the change is needed.
- Run focused tests during the loop, then relevant suite checks before commit.
- If you touch business logic without adding or updating tests, explain why in the PR or final handoff.
