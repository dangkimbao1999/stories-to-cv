# Project Memory

Store durable project memory here.

Good memory:

- architectural decisions
- recurring debugging patterns
- deployment gotchas
- testing conventions that should not be rediscovered

Bad memory:

- temporary scratch notes
- task-local TODOs that will be obsolete tomorrow

Use `pnpm ops:memory:add -- decisions "..."` to append a note quickly.
