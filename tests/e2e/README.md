# E2E

Playwright end-to-end tests live here.

Goals:

- protect major user-facing flows
- keep a small smoke suite runnable on every branch
- make local debugging easy with trace, screenshot, and headed mode

Before the main product app is scaffolded, the initial smoke spec is marked `fixme`.
